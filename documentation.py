'''

--all functions--


-geometry.cal():


• geometry.cal_hyp(side_a, side_b)

-> calculates the hypotenuse based on the parameters given

-> returns(hypotenuse)


• geometry.cal_side_other(side_a, hypotenuse)

-> calculates the length of the other side based on the parameters given

-> returns(length_of_other_side)


• geometry.cal_perimeter(list/tuple_of_side_lengths)

-> calculates the perimeter based on the values stored in a list/tuple given

-> returns(perimeter)


• geometry.cal_remaining_perimeter(list/tuple_of_side_lengths, total_length, total_sides)

-> calculates remaining length of sides and remaing of undefined sides based on values given

-> returns(remaining_length, remaining_undefined_sides)


• geometry.cal_cirumference(radius)

-> calculates the circumference based on the radius given

-> returns(circumference)


• geometry.cal_radius(circumference)

-> calculates the radius based on the circumference given

-> returns(radius)


• geometry.cal_area_sqr(width)

-> calculates the area of a square based on the parameters given

-> returns(area)


• geometry.cal_area_tri(width, height)

-> calculates the area of a tri based on the parameters given

-> returns(area)


• geometry.cal_area_rect(width, height)

-> calculates the area of a rectangles based on the parameters given

-> returns(area)


• geometry.cal_area_circle(radius)

-> calculates the area of a circle based on the parameters given

-> returns(area)


• geometry.cal_area_para(width, height)

-> calculates the area of a parallelogram based on the parameters given

-> returns(area)


• geometry.cal_area_kite(width, height)

-> calculates the area of a kite based on the parameters given

-> returns(area)


• geometry.cal_area_trap(width_a, width_b, height)

-> calculates the area of a trapezium based on the parameters given

-> returns(area)


• geometry.cal_surf_cube(length)

-> calculates the surface area of a cube based on the parameters given

-> returns(area)


• geometry.cal_surf_cuboid(length, width, height)

-> calculates the surface area of a cuboid based on the parameters given

-> returns(area)


• geometry.cal_surf_prism(length, width, height, slant)

-> calculates the surface of a prism based on the parameters given

-> returns(area)


• geometry.cal_surf_pyramid(length, slant)

-> calculates the surface area of a pyramid based on the parameters given

-> returns(area)


• geometry.cal_surf_cylinder(radius, height)

-> calculates the surface area of a cylinder based on the parameters given

-> returns(area)


• geometry.cal_surf_cone(radius, slant)

-> calculates the surface area of a cone based on the parameters given

-> returns(area)


• geometry.cal_surf_sphere(radius)

-> calculates the surface area of a circle based on the parameters given

-> returns(area)


•geometry.cal_angles(tuple/list_of_angles, total_sides)

-> calculates the total of angles based on values given

-> returns(total_angles)


•geometry.cal_remaining_angles(tuple/list_of_angles, total_sides)

-> calculates the remaining total of angles, and number of angles with undefined values based on values given

-> returns(remaining_angles, remaining_number_of_angles)


•geometry.cal_volume_cube(length, width, height)

-> calculates the volume of a cube based on parameters given

-> returns(volume)


•geometry.cal_volume_cuboid(length, width, height)

-> calculates the volume of a cuboid based on parameters given

-> returns(volume)


•geometry.cal_volume_prism(length, width, height)

-> calculates the volume of a prism based on parameters given

-> returns(volume)


•geometry.cal_volume_pyramid(length, width, height)

-> calculates the volume of a pyramid based on parameters given

-> returns(volume)


•geometry.cal_volume_cylinder(radius, height)

-> calculates the volume of a cylinder based on parameters given

-> returns(volume)


•geometry.cal_volume_cone(radius, height)

-> calculates the volume of a cone based on parameters given

-> returns(volume)


•geometry.cal_volume_sphere(radius)

-> calculates the volume of a sphere based on parameters given

-> returns(volume)



-geometry.convert():

	
• geometry.convert_radius_diameter(diameter)

-> converts diameter to radius based on the diameter given

-> returns(radius)


• geometry.convert_diameter_radius(radius)

-> converts radius to diameter based on the radius given

-> returns(diameter)